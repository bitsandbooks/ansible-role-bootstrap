---
packages_installed:
  - epel-release
  - firewalld    # firewall
  - vim-enhanced # text editor of choice
  - zsh          # shell of choice
packages_removed:
  - cloud-init   # unneeded after install
